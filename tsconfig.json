{
  "compilerOptions": {
    /* Opciones Base */
    "target": "ES2020", // Compila a una versión moderna de JS
    "module": "CommonJS", // Le dice a TS que GENERE CommonJS para Node.js
    "lib": ["ES2020"],
    "strict": true, // Habilita todas las comprobaciones estrictas de tipos
    "skipLibCheck": true, // Evita revisar los .d.ts de las librerías

    /* Opciones de Módulos (La parte importante) */
    "moduleResolution": "Node", // El estándar para Node.js
    "esModuleInterop": true, // La "magia" para que 'import express from "express"' funcione
    "allowSyntheticDefaultImports": true,
    "verbatimModuleSyntax": false, // <-- LA CORRECCIÓN DIRECTA A TU ERROR

    /* Opciones del Proyecto */
    "rootDir": "./src",
    "outDir": "./dist", // Donde irá el código compilado
    "sourceMap": true // Útil para debugging
  },
  "include": ["src/**/*"], // Solo compila lo que está en la carpeta 'src'
  "exclude": ["node_modules"]
}
